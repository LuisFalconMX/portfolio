.button {
  @apply inline-flex;
  @apply py-3 px-4 mr-2 md:px-7 rounded;
  @apply bg-primary text-light;
  @apply text-sm md:text-base font-semibold;
}

.button__icon {
  @apply mr-2;
}

.button__icon--right {
  @apply flex-row-reverse;
}

.button__icon--right .button__icon {
  @apply mr-0 ml-2;
}
